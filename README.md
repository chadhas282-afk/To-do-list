# 📝 Modern Task Manager

A sleek, minimalist To-Do List application built with vanilla JavaScript. This project focuses on a clean user experience with smooth transitions and a responsive design.

---

## ✨ Features

* **Clean UI/UX:** Uses the 'Inter' font family and a soft color palette for a professional look.
* **Interactive Tasks:** Add tasks instantly and remove them with a smooth fade-out animation.
* **Hover Effects:** Subtle elevation changes when interacting with task items.
* **Fully Responsive:** Designed to work seamlessly on both desktop and mobile browsers.

## 🚀 Getting Started

Since this is a client-side application, you don't need to install any dependencies.

1. **Clone the repository:**
```bash
git clone https://github.com/chadhas282-afk/To-do-list

```


2. **Navigate to the folder:**
```bash
cd task-manager

```


3. **Launch the app:**
Simply open `index.html` in your favorite web browser.

## 📂 Project Structure

```text
├── index.html   # Main layout and structure
├── style.css    # Custom styling and animations
└── script.js    # Task handling logic and DOM manipulation

```

## 🛠️ Customization

To change the primary theme color (currently Blue), update the following variable in `style.css`:

```css
#addTaskButton {
    background-color: #0984e3; 
}

```


### 📝 To-Do / Future Improvements

* [ ] Add LocalStorage support to persist tasks after refresh.
* [ ] Add a "Mark as Complete" toggle.
* [ ] Implement drag-and-drop reordering.